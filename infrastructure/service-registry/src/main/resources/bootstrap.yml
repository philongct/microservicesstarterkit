spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://localhost:8888

server:
  ssl:
    enabled: false

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0