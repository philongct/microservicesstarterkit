spring:
  application:
    name: transaction-service
  cloud:
    config:
      http://localhost:8888

server:
  port: 6000

---
spring:
  profiles: peer
server:
  # Auto assigned
  port: 0
